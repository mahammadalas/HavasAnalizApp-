import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationView {
            VStack(spacing: 20) {
                Text("🔮 Havas ve Ezoterik Kader Analizi")
                    .font(.largeTitle)
                    .fontWeight(.bold)
                    .multilineTextAlignment(.center)
                    .padding()
                
                VStack(spacing: 15) {
                    NavigationLink(destination: KullaniciBilgiView()) {
                        MenuButton(title: "Genel Analiz", icon: "person.text.rectangle")
                    }
                    
                    NavigationLink(destination: AskAnaliziView()) {
                        MenuButton(title: "Aşk ve İlişki Analizi", icon: "heart.fill")
                    }
                    
                    NavigationLink(destination: RuhsalGucView()) {
                        MenuButton(title: "Ruhsal Güçler", icon: "sparkles")
                    }
                    
                    NavigationLink(destination: KaderAnaliziView()) {
                        MenuButton(title: "Kader Analizi", icon: "star.circle.fill")
                    }
                    
                    NavigationLink(destination: HavasBilgiView()) {
                        MenuButton(title: "Havas Bilgileri & Ritüeller", icon: "book.fill")
                    }
                }
                .padding()
            }
        }
    }
}

struct KullaniciBilgiView: View {
    @State private var isim: String = ""
    @State private var soyad: String = ""
    @State private var dogumTarihi: String = ""
    @State private var anneAdi: String = ""
    @State private var cinsiyet: String = ""
    
    let cinsiyetSecenekleri = ["Erkek", "Kadın"]
    
    var body: some View {
        VStack(spacing: 15) {
            Text("🔮 Kendi Bilgilerini Gir")
                .font(.title)
                .fontWeight(.bold)
                .padding()
            
            TextField("İsim", text: $isim)
                .textFieldStyle(RoundedBorderTextFieldStyle())
                .padding()
            
            TextField("Soyad", text: $soyad)
                .textFieldStyle(RoundedBorderTextFieldStyle())
                .padding()
            
            TextField("Doğum Tarihi (GG.AA.YYYY)", text: $dogumTarihi)
                .textFieldStyle(RoundedBorderTextFieldStyle())
                .padding()
            
            TextField("Anne Adı", text: $anneAdi)
                .textFieldStyle(RoundedBorderTextFieldStyle())
                .padding()
            
            Picker("Cinsiyet", selection: $cinsiyet) {
                ForEach(cinsiyetSecenekleri, id: \ .self) { secenek in
                    Text(secenek)
                }
            }
            .pickerStyle(SegmentedPickerStyle())
            .padding()
            
            NavigationLink(destination: GenelAnalizView(isim: isim, soyad: soyad, dogumTarihi: dogumTarihi, anneAdi: anneAdi, cinsiyet: cinsiyet)) {
                Text("Analizi Başlat")
                    .foregroundColor(.white)
                    .frame(maxWidth: .infinity, minHeight: 50)
                    .background(Color.purple)
                    .cornerRadius(12)
                    .padding()
            }
        }
    }
}

struct GenelAnalizView: View {
    var isim: String
    var soyad: String
    var dogumTarihi: String
    var anneAdi: String
    var cinsiyet: String
    
    var body: some View {
        VStack(spacing: 15) {
            Text("🔮 Genel Analiz Sonucu")
                .font(.title)
                .fontWeight(.bold)
                .padding()
            
            Text("İsim: \(isim) \(soyad)")
            Text("Doğum Tarihi: \(dogumTarihi)")
            Text("Anne Adı: \(anneAdi)")
            Text("Cinsiyet: \(cinsiyet)")
            
            Divider()
            
            Text("✨ Havas Analizi: İsminizin enerjisi yüksek. Ruhsal gücünüz fark edilmenizi sağlıyor.")
                .padding()
            
            Text("📜 Ebced Değeriniz: 732 (Örnek Değer)")
                .padding()
            
            Text("🌟 Kader Yıldızınız: Venüs ve Ay")
                .padding()
        }
    }
}

struct MenuButton: View {
    var title: String
    var icon: String
    
    var body: some View {
        HStack {
            Image(systemName: icon)
                .foregroundColor(.white)
                .font(.title2)
            Text(title)
                .font(.title3)
                .fontWeight(.semibold)
                .foregroundColor(.white)
        }
        .frame(maxWidth: .infinity, minHeight: 60)
        .background(Color.black.opacity(0.8))
        .cornerRadius(12)
        .padding(.horizontal)
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
